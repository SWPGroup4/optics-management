spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/optics_test
    username: test
    password: test
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
